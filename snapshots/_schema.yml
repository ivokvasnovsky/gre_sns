version: 2

snapshots:
  - name: gre_sns_snapshot
    description: "Snapshot of the GRE SNS staging table to track historical changes"
    config:
      target_schema: mart
      strategy: check
      unique_key: gre_sns_unique_key  
      check_cols: all           
      hard_deletes: invalidate